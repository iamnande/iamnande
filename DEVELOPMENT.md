# uwu nuxti wuxi

## setup

install dependencies:

```bash
pnpm install
```

## dev server

start the dev server on `http://localhost:3000`:

```bash
# npm
pnpm dev
```

## prod build

build the application for prod:

```bash
pnpm build
```

local preview (prod) build:

```bash
pnpm preview
```
